#include<bits/stdc++.h>
#define endl '\n'
#define OuO ios::sync_with_stdio(0);cin.tie(0);
using namespace std;

signed main(){OuO
    long long a, b;
    cin >> a >> b;
    cout << ((a & 1) ? "True" : "False");
}